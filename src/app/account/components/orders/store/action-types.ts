export enum ActionTypes {
  GET_ORDERS = '[User orders] Get user orders',
  GET_ORDERS_SUCCESS = '[User orders] Get user orders success',
  GET_ORDERS_FAILURE = '[User orders] Get user orders failure',
}
